---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: recurringsilences.observability.giantswarm.io
spec:
  group: observability.giantswarm.io
  names:
    kind: RecurringSilence
    listKind: RecurringSilenceList
    plural: recurringsilences
    singular: recurringsilence
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: RecurringSilence is the Schema for the recurringsilences API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RecurringSilenceSpec defines the desired state of RecurringSilence.
            properties:
              schedule:
                description: |-
                  Schedule is the cron expression for the recurring silence.
                  It follows the format of https://github.com/aptible/supercronic.
                type: string
              silenceTemplate:
                description: SilenceTemplate defines the template for the Silence
                  object to be created.
                properties:
                  matchers:
                    description: Matchers defines the alert matchers that this silence
                      will apply to.
                    items:
                      description: SilenceMatcher defines an alert matcher to be muted
                        by the Silence.
                      properties:
                        matchType:
                          default: =
                          description: MatchType defines the type of matching to perform.
                          enum:
                          - =
                          - '!='
                          - =~
                          - '!~'
                          type: string
                        name:
                          description: Name of the label to match.
                          maxLength: 256
                          minLength: 1
                          type: string
                        value:
                          description: Value to match for the given label name.
                          maxLength: 1024
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    minItems: 1
                    type: array
                required:
                - matchers
                type: object
              timeZone:
                description: |-
                  TimeZone for the cron schedule.
                  If not specified, the controller will use its local time zone.
                type: string
            required:
            - schedule
            - silenceTemplate
            type: object
          status:
            description: RecurringSilenceStatus defines the observed state of RecurringSilence.
            properties:
              active:
                description: Active is a list of references to Silence objects created
                  by this RecurringSilence.
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              lastScheduleTime:
                description: LastScheduleTime is the last time a Silence was successfully
                  created.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
