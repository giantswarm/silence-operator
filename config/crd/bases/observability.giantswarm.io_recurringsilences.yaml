---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: recurringsilences.observability.giantswarm.io
spec:
  group: observability.giantswarm.io
  names:
    kind: RecurringSilence
    listKind: RecurringSilenceList
    plural: recurringsilences
    singular: recurringsilence
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.duration
      name: Duration
      type: string
    - jsonPath: .status.activeSilence
      name: Active Silence
      type: string
    - jsonPath: .status.nextScheduledTime
      name: Next Scheduled
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: RecurringSilence is the Schema for the recurring silences API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RecurringSilenceSpec defines the desired state of RecurringSilence.
            properties:
              duration:
                description: |-
                  Duration specifies how long each silence should last.
                  Must be a valid Go duration string (e.g., "30m", "2h", "24h").
                pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                type: string
              matchers:
                description: Matchers defines the alert matchers that the generated
                  silences will apply to.
                items:
                  description: SilenceMatcher defines an alert matcher to be muted
                    by the Silence.
                  properties:
                    matchType:
                      default: =
                      description: MatchType defines the type of matching to perform.
                      enum:
                      - =
                      - '!='
                      - =~
                      - '!~'
                      type: string
                    name:
                      description: Name of the label to match.
                      maxLength: 256
                      minLength: 1
                      type: string
                    value:
                      description: Value to match for the given label name.
                      maxLength: 1024
                      type: string
                  required:
                  - name
                  - value
                  type: object
                minItems: 1
                type: array
              schedule:
                description: |-
                  Schedule defines when the silence should be created using cron expression format.
                  Supports 5-field format: "* * * * *" (minute hour day-of-month month day-of-week).
                  Examples:
                    - "0 0 * * *" - Daily at midnight
                    - "0 2 * * 1" - Weekly on Monday at 2 AM
                    - "30 14 1 * *" - Monthly on the 1st at 2:30 PM
                pattern: ^(\S+\s+\S+\s+\S+\s+\S+\s+\S+)$
                type: string
            required:
            - duration
            - matchers
            - schedule
            type: object
          status:
            description: RecurringSilenceStatus defines the observed state of RecurringSilence.
            properties:
              activeSilence:
                description: ActiveSilence indicates the name of the currently active
                  silence created by this RecurringSilence.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the RecurringSilence's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastScheduledTime:
                description: LastScheduledTime indicates the last time a silence was
                  scheduled from this RecurringSilence.
                format: date-time
                type: string
              nextScheduledTime:
                description: NextScheduledTime indicates the next time a silence will
                  be scheduled from this RecurringSilence.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
